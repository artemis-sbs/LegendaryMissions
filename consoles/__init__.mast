import standard_consoles.mast
import common_console_select.mast
import manual_weapons.mast
import gui_screen_tabs.py
import results_helpers.py
import manual_beams_helpers.py
import tether_indicator.py
import game_results.mast

import server_console.py
import server_console.mast
import media_music.mast

# Engineering's View tab. Before eng_grid_panel.py, which mounts it as a tab.
import eng_view.py
# Engineering's right column. Before layout_widgets, which calls eng_grid_panel().
import eng_grid_panel.py
# The comms filter chips above the 2D view. Before layout_widgets, which builds them.
import comms_chips.py
# Engineering's crew rail above the interior view. Before layout_widgets too.
import eng_crew_chips.py
# Engineering's grid orders - the //comms/grid buttons, drawn by the console.
import eng_grid_buttons.py

# The prototype science console (SCIENCE_NEW_LAYOUT). Queue first - both of the others
# import it - then the tab listbox, which the panel imports for the current tab.
# The scan-time model - engine fields first. Before science_queue, which calls it.
import science_scan_time.py
import science_queue.py
import science_tabs.py
import science_panel.py
# The filter chips under the science 2D view. Before science_layout, which draws them.
import science_chips.py
# The queue list with its Move up / Cancel buttons. Before science_layout draws it.
import science_queue_list.py

import layout_widgets.mast
# The prototype science console's own layout label, jumped to from //gui/normal_sci.
import science_layout.mast


import debug.mast #want debug tabs left of upgrade
import brain_scan.mast

# Last: it only adds a route and registrations, so it cannot move the classic
# strip's order, which is load order.
import epadd.py
import epadd.mast
import offer_digest.py
import offers.mast
