#
# Legacy per-item pickup collision chain removed - replaced by the single
# generic route in the items addon (items/item_collect.mast), which reads the
# item key stored on the pickup and credits the collector. Spawners now tag
# pickups with the "item" role, so collection is handled there.
#
